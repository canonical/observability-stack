name: Test the Terraform product module
run-name: Deploy ${{ inputs.product }} on ${{ inputs.runner }}

on:
  workflow_dispatch:
    inputs:
      product:
        description: 'The name of the Terraform product module to test.'
        required: true
        type: choice
        default: cos
        options:
        - cos
        - cos_lite
        - cos_dev
      runner:
        description: 'The runner label to use for the job.'
        type: choice
        default: ubuntu-latest
        options:
        - ubuntu-latest
        - self-hosted-linux-amd64-noble-large
        - self-hosted-linux-amd64-noble-xlarge
        - self-hosted-linux-amd64-noble-2xlarge
      debug-ssh:
        description: 'Open an SSH session once the substrate is provisioned, before the product tests run.'
        type: boolean
        default: false
  workflow_call:
    inputs:
      product:
        required: true
        type: string
      runner:
        type: string
        default: ubuntu-latest
      debug-ssh:
        type: boolean
        default: false

jobs:
  terraform-test:
    name: Test product module
    runs-on: ${{ inputs.runner }}
    strategy:
      fail-fast: false
      matrix:
        juju-channel: ["3.6/stable"]
        scenario: ["tls_none"]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # https://documentation.ubuntu.com/self-hosted-runners/en/latest/usage/faq/how-to-setup-canonical-k8s/
      - name: Remove docker.io and containerd
        if: ${{ runner.environment == 'self-hosted' }}
        run: |
          export DEBIAN_FRONTEND=noninteractive
          sudo apt-get remove -y docker.io containerd
          sudo rm -rf /run/containerd

      - name: Concierge prepare
        timeout-minutes: 30
        run: |
          sudo snap install concierge --classic
          # -E so concierge can expand $DOCKERHUB_MIRROR from the runner's env.
          sudo -E concierge prepare \
            --config concierge-k8s.yaml \
            --juju-channel ${{ matrix.juju-channel }} \
            --extra-snaps just,astral-uv,terraform

      # The one setting that cannot live in concierge-k8s.yaml: concierge expands
      # environment variables only in the image-registry block, not in `features`,
      # and the right value depends on the machine the job landed on.
      - name: Point CoreDNS at the node's resolvers
        timeout-minutes: 10
        run: |
          # CoreDNS forwards to the node's /etc/resolv.conf unless told otherwise,
          # but on Ubuntu that file is the systemd-resolved stub (127.0.0.53),
          # which inside the CoreDNS pod is the pod's own loopback. The real
          # resolvers are only in /run/systemd/resolve/resolv.conf. MicroK8s' dns
          # addon probed for these; Canonical K8s does not.
          DNS_SOURCES=""
          for f in /run/systemd/resolve/resolv.conf /etc/resolv.conf; do
            [ -r "$f" ] && DNS_SOURCES="$DNS_SOURCES $f"
          done
          # Word splitting of $DNS_SOURCES is intentional.
          # shellcheck disable=SC2086
          UPSTREAM_DNS=$(awk '/^nameserver/ && $2 !~ /^127\./ {print $2}' $DNS_SOURCES \
            | awk '!seen[$0]++' | paste -sd,)
          if [ -z "$UPSTREAM_DNS" ]; then
            echo "::error::Could not determine the node's upstream nameservers from:$DNS_SOURCES"
            exit 1
          fi
          echo "Setting dns.upstream-nameservers to ${UPSTREAM_DNS}"
          sudo k8s set dns.upstream-nameservers="$UPSTREAM_DNS"
          echo "UPSTREAM_DNS=${UPSTREAM_DNS}" >> "$GITHUB_ENV"

          sudo k8s status --wait-ready --timeout 5m

      # Establishes *where* name resolution breaks before spending 5+ minutes on a
      # deployment that would fail opaquely on every charm at once. The four probes
      # separate the possible causes:
      #   1 fails            -> pod cannot reach the CoreDNS ClusterIP (datapath)
      #   1 ok, 2 fails      -> CoreDNS reachable, its upstream forwarding is broken
      #   3 ok, 2 fails      -> upstream is reachable from pods; CoreDNS config wrong
      #   3 fails, 4 ok      -> node's resolver unreachable from pods; use a public one
      #   3 and 4 both fail  -> pod egress is blocked, not a DNS problem at all
      - name: Diagnose in-cluster DNS
        timeout-minutes: 6
        run: |
          echo "--- CoreDNS Corefile ---"
          kubectl -n kube-system get configmap coredns -o jsonpath='{.data.Corefile}' || true
          echo
          echo "--- CoreDNS service and endpoints ---"
          kubectl -n kube-system get svc,endpoints -l k8s-app=kube-dns -o wide || true

          kubectl -n kube-system rollout status deployment/coredns --timeout=3m

          # nslookup takes a single server, so use the first of the list.
          NODE_DNS="${UPSTREAM_DNS%%,*}"
          echo "Probing with node resolver ${NODE_DNS}"

          kubectl run dns-check --restart=Never --image=busybox:1.36 --command -- \
            sh -c "
              echo '== 1. internal name via cluster DNS =='
              nslookup kubernetes.default.svc.cluster.local; echo \"rc=\$?\"
              echo '== 2. external name via cluster DNS =='
              nslookup api.charmhub.io; rc_cluster=\$?; echo \"rc=\$rc_cluster\"
              echo '== 3. external name direct to node resolver ${NODE_DNS} =='
              nslookup api.charmhub.io ${NODE_DNS}; echo \"rc=\$?\"
              echo '== 4. external name direct to 8.8.8.8 =='
              nslookup api.charmhub.io 8.8.8.8; echo \"rc=\$?\"
              exit \$rc_cluster
            "
          kubectl wait --for=jsonpath='{.status.phase}'=Succeeded pod/dns-check --timeout=3m || true

          echo "--- dns-check output ---"
          kubectl logs dns-check || true
          phase=$(kubectl get pod dns-check -o jsonpath='{.status.phase}' 2>/dev/null || echo Unknown)
          kubectl delete pod dns-check --now --ignore-not-found

          if [ "$phase" != "Succeeded" ]; then
            echo "::error::Pods cannot resolve external names (phase=$phase). See the probe results above."
            exit 1
          fi

      - name: Install and configure microceph
        if: ${{ inputs.product == 'cos' }}
        run: |
          # https://github.com/canonical/microceph-action/blob/main/microceph.sh
          function check_ceph_ok_or_exit () {
              i=0
              for i in {1..5}; do
                  if sudo microceph.ceph status | grep HEALTH_OK; then
                      break
                  else
                      sudo microceph.ceph status
                      sleep 30
                      sudo microceph.ceph health detail
                  fi
              done
              if [ "$i" -eq 5 ]; then
                  exit 1
              fi
          }

          sudo snap install microceph
          sudo microceph cluster bootstrap
          sleep 30s
          sudo microceph.ceph config set "mon.$(hostname)" mon_data_avail_warn 6
          sudo microceph disk add loop,2G,3
          check_ceph_ok_or_exit

          sudo microceph enable rgw --port 8080 --ssl-port 8443
          sudo microceph.radosgw-admin user create --uid=user --display-name=User
          sudo microceph.radosgw-admin key create --uid=user --key-type=s3 --access-key=access-key --secret-key=secret-key

      - name: Report substrate state
        run: |
          sudo k8s status
          kubectl get nodes -o wide
          kubectl get pods --all-namespaces
          juju controllers
          juju clouds --client

      # Provisioning is complete but no product has been deployed yet, so this is
      # the cheapest point at which to inspect a freshly built substrate.
      - name: Open SSH session before the product tests
        if: ${{ inputs.debug-ssh || runner.debug == '1' }}
        uses: mxschmitt/action-tmate@v3
        with:
          timeout-minutes: 30
          limit-access-to-actor: true

      - name: Test deployment
        run: |
          if [[ "${{ inputs.product }}" == "cos" ]]; then
            S3_ENDPOINT=http://$(ip -4 -j route get 2.2.2.2 | jq -r '.[] | .prefsrc'):8080
            export S3_ENDPOINT
            export S3_ACCESS_KEY=access-key
            export S3_SECRET_KEY=secret-key
          fi
          if [[ "${{ runner.debug }}" == "1" ]]; then
            export KEEP_MODELS=true
            just integration ${{ inputs.product }}/${{ matrix.scenario }} --keep-models
          else
            just integration ${{ inputs.product }}/${{ matrix.scenario }}
          fi

      - name: Open SSH session on failure
        if: ${{ failure() && (inputs.debug-ssh || runner.debug == '1') }}
        uses: mxschmitt/action-tmate@v3
        with:
          timeout-minutes: 30
          limit-access-to-actor: true
